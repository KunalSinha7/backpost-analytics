# Required when running the frontend locally (outside Docker).
# Points the API client at the local backend dev server.
VITE_API_URL=http://localhost:8000

# Required for Playwright tests (password reset flow hits Mailpit directly).
MAILPIT_HOST=http://localhost:8025
